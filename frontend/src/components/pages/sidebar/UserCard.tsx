import React from "react";
import { TUser } from "@/types/user";

type Props = {
  user: TUser
}

export const UserCard: React.FC<Props> = ({ user }) => {
  return (
    <div>
      
    </div>
  );
}